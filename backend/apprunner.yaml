version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: gunicorn config.wsgi:application --worker-tmp-dir /dev/shm --timeout 60
  network:
    port: 8000
